CREATE TABLE IF NOT EXISTS EVENT (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    come INTEGER,
    answer INTEGER,
    tobiiri INTEGER,
    kikenn INTEGER,
    notcome INTEGER,
    newevent INTEGER,
    student INTEGER,
    event_name TEXT,  
    year_value INTEGER
);